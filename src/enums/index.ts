export enum ENav {
  Messenger = 'Messenger',
  Friend = 'Friend',
  Settings = 'Settings',
}

export enum EFriendStatus {
  Waiting = 'Waiting',
  Accept = 'Accept',
  Add = 'Add',
  Friend = 'Friend',
}
